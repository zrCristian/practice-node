const colors = require("colors")

console.log("Hello world".bgBlue)
console.log("probando archivo ejecutable de nodemon".bgCyan)
console.log("cristian".rainbow)
console.log("cristian".random)


// npx sirve para ejecutar programas/app (paquetes) desde la consola que pueden instalarse con node
// Es decir usamos modulos que funcionen como CLI (Common Line Interface o Herr que funcionan desde consola) sin la necesidad de instalarlos globalmente ni en la carpete "package.json / node_modules"